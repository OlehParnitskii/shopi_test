{% comment %}<div class="page-width">{% endcomment %}
  {% comment %}<div class="empty-page-content text-center">{% endcomment %}
    {% comment %}<h1>{{ 'general.404.title' | t }}</h1>{% endcomment %}
    {% comment %}<p>{{ 'general.404.subtext' | t }}</p>{% endcomment %}
    {% comment %}<p>{% endcomment %}
      {% comment %}<a href="{{ routes.root_url }}" class="btn btn--has-icon-after">{{ 'general.404.link' | t }}{% include 'icon-arrow-right' %}</a>{% endcomment %}
    {% comment %}</p>{% endcomment %}
  {% comment %}</div>{% endcomment %}
{% comment %}</div>{% endcomment %}


<div class="not-found" style="background-image: url('img/404-img.jpg')">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-auto">
        <div class="not-found__box">
          <div class="not-found__box-title">
            <h1>Помилка</h1>
          </div>
          <div class="not-found__box-number">404</div>
          <div class="not-found__box-text">Сторінка не знайдена</div>
          <div class="not-found__box-link">
            <a href="{{ routes.root_url }}" class="btn btn-black">На головну</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

